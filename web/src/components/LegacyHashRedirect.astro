<script>
  import { legacyHashTarget } from "../lib/routes";

  const target = legacyHashTarget(window.location.hash, import.meta.env.BASE_URL);
  if (target) {
    window.location.replace(target);
  }
</script>
